export type City = {
  latitude: number;
  longitude: number;
  zoom: number;
}

export type Point = {
    latitude: number;
    longitude: number;
    zoom: number;
  }

export type Points = Point[];
